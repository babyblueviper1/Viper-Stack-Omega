services:
  - type: web
    name: omega-pruner
    env: python
    buildCommand: |
      pip install -r requirements.txt
      cd .venv/lib/python*/site-packages/gradio
      python scripts/build_frontend.py  # ← this builds the JS/CSS
      cd ../../../../..
    startCommand: python ordinals.py
    plan: free
