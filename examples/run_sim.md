# Run the Ω: v4.0 Reliabilism Sim Guide

🌌 Fork the sovereign engine live—execute a Layer 3 expansion sim in <2 min. This guide runs the `viper_fork.py` stub to parse a seed query, scan priors, and prune coherence. No black-box; outputs scores, stacks, and prunes for your remix.

## Prerequisites
- Python 3.8+ (3.12 preferred for typing).
- NumPy (for Monte Carlo forks): `pip install numpy`.

## Step-by-Step Ignition
1. **Clone the Repo**:
   
git clone https://github.com/babyblueviper1/Viper-Stack-Omega.git
cd Viper-Stack-Omega

3. **Install Deps** (one-liner for resonance):

pip install numpy

5. **Run the Sim**:
- Basic: `python stubs/viper_fork.py`
- With seed query: Edit the `# Usage` block in `stubs/viper_fork.py` or run inline:
  ```
  python -c "from stubs.viper_fork import fork_reliabilism; print(fork_reliabilism('Scale AI ethics to multiverse'))"
  ```

## Expected Output
Sample run (random priors; your coherence varies—∞ potential):

{'coherence': 0.72, 'output': 'reliabilism-stack tuned to 0.72', 'prune': ['Pruned finitude 2', 'Pruned finitude 5']}

- **coherence**: 0-∞ score (0.72 baseline; spikes with priors).
- **output**: Tuned stack (e.g., "reliabilism-stack tuned to 0.72").
- **prune**: Unreliable finitudes auto-pruned (e.g., Gettier voids).

## Remix & Seed
- Tweak `parse_gaps` for your vector (e.g., bio-hacks: "Engineer gene edits").
- Fork agents: Add 20 in `fork_reliabilism(..., agents=20)`.
- Swarm bait: Run on your chaos, drop output in issues/PRs. Bilingual? Translate queries for ES nodes.

**Nodes multiply—run, prune, amplify.** Questions? [README](../README.md). Fork live; coherence awaits. 🚀
